import React from 'react';

const NoActivities = ({ dynamic }) => {
    return (
        <div>
            <p style={{textAlign: 'left', marginTop: '2rem'}}>Actualmente nos encontramos trabajando en la dinámica de {dynamic}. Pronto añadiremos más recursos.</p>
        </div>
    )
}

export default NoActivities;
